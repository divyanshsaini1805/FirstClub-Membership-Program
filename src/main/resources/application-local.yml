spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/membership
    username: membership
    password: membership
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.firstclub: DEBUG
    org.hibernate.SQL: DEBUG
